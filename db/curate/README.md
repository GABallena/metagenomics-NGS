# db/curate

Curation utilities that:
- standardize FASTA headers into deterministic, tool-friendly IDs
- emit metadata TSV mapping new IDs → original IDs/headers/provenance

Subfolders:
- `amr/`, `virulence/`, `mobilome/`, `pathogens/`

Keep curated outputs outside git or in ignored paths.
